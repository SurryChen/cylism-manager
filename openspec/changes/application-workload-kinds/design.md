## Decisions

控制器类型属于应用而非上线模板，同一应用的各次发布必须使用同一种控制器。StatefulSet 直接引用既有 PVC；带 PVC 的应用维持单副本约束。首次成功发布前只更新类型元数据，不执行资源迁移。

迁移从最近一次成功发布的不可变快照恢复期望状态。平台先将旧控制器缩为零并等待 Pod 停止，再创建新控制器并等待就绪。旧控制器保留为零副本以支持失败恢复；切换成功后只更新应用类型。

## Risk

迁移有短暂服务中断。没有成功发布记录、使用多副本 PVC 或当前控制器不受平台管理时拒绝切换。
