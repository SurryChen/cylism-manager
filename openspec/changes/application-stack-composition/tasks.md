# Tasks

1. [x] 编写模型与 Store 测试：环境级栈模板、栈发布、组件应用归属及删除引用保护。
2. [x] 替换应用内栈模型为环境级栈模型，移除复合资源渲染与应用详情下的栈 API。
3. [x] 编写并实现应用栈 API：模板 CRUD、发布、组件应用创建/复用和发布状态聚合。
4. [x] 编写 Karakeep 预设测试并实现预设实例化，生成三个独立 Application 与对应单应用模板。
5. [x] 调整工作台与路由，新增环境级应用栈页面/区域；从应用详情移除栈组件 UI。
6. [x] 编写前端测试：栈列表、Karakeep 预设创建、组件独立版本发布和入口应用跳转。
7. [x] 运行 `gofmt`、`go test ./...`、`go build ./...`、`npm --prefix web test -- --run`、`npm --prefix web run build`。
8. [ ] `sec-code` 安全扫描工具在当前主机缺失，待恢复后上报；验证通过后等待用户确认是否归档。
