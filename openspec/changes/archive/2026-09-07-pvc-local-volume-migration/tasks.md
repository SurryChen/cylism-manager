## 1. 数据模型与 Kubernetes 原语

- [x] 1.1 为迁移状态、任务快照、复制进度和源卷清理状态建立 GORM 模型、Store 方法及单元测试。
- [x] 1.2 扩展 PVC/PV 客户端，验证 local path、读取 PV 本地路径、创建/删除迁移目标 PVC 与临时绑定 Pod，并编写 fake client 测试。
- [x] 1.3 增加 Deployment 停止、Pod 终止等待、模板/PVC 引用枚举及迁移锁校验，并覆盖受控单应用限制。
- [x] 1.4 更新 `k8s/platform-deployment.yaml` 的最小 RBAC，允许临时 Pod 生命周期和迁移必需的 PVC/PV 只读操作。

## 2. 迁移编排与安全复制

- [x] 2.1 编写 SSH 参数、路径转义、磁盘/tar 预检与双 SSH 流式 tar 复制测试，确保不记录密钥或敏感命令内容。
- [x] 2.2 实现迁移服务的预检、目标预配、停止源工作负载、复制、切换、就绪等待、回滚和重启后收敛。
- [x] 2.3 实现内部迁移 Release 与关联模板更新，验证历史 Release 快照不被修改且后续发布使用目标 PVC。
- [x] 2.4 实现迁移详情、步骤日志、源卷显式清理 API 和 API 测试，覆盖所有失败阶段和并发锁定。

## 3. 存储与应用界面

- [x] 3.1 为 PVC 创建表单编写前端测试，并将容量改为数字输入加 Mi/Gi/Ti 单位选择。
- [x] 3.2 为存储卷页面编写迁移选择、阶段轮询、失败诊断、回滚状态与清理确认测试。
- [x] 3.3 实现迁移对话框和迁移任务展示，使用既有 Design Token、无障碍菜单与确认交互。
- [x] 3.4 在应用模板与发布界面展示迁移锁和当前迁移中的 PVC，阻止冲突操作。

## 4. 验证与运维

- [x] 4.1 运行 `gofmt`、相关 Go 单元/API 测试和全量 `go test ./...`，并呈报每个任务的验证结果。
- [x] 4.2 运行前端聚焦测试、全量 `npm --prefix web test -- --run` 与 `npm --prefix web run build`。
- [x] 4.3 执行每个修改业务文件的 sec-code 安全扫描上报，并记录扫描不可用时的原因。
