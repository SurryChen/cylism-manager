## 1. Layout regression coverage

- [x] 1.1 为 Dashboard 添加失败的布局测试，验证常见桌面视口下默认四个快捷入口不形成局部滚动，且关键首页区块处于首屏布局。
- [x] 1.2 为窄屏和矮屏添加失败的布局测试，验证页面自然滚动降级及快捷入口 44px 最小点击高度。

## 2. Dashboard first-screen layout

- [x] 2.1 调整 Dashboard 桌面端网格的垂直空间预算和必要的卡片间距，使默认内容符合首屏高度目标。
- [x] 2.2 调整快捷操作列表的溢出策略：常见桌面视口不使用局部滚动，受限视口保留完整内容与自然页面滚动。
- [x] 2.3 运行聚焦 Dashboard 测试并在 1440 x 900、窄屏和矮屏布局下验证无裁剪、无默认快捷列表滚动。
- [x] 2.4 利用首行新增空间调整概览和运行检查的排版与字号，避免不必要的卡片留白和过小的状态文本。
- [x] 2.5 统一首行卡片标题栏与内边距，并重排集群、应用和运行检查内容，移除常见桌面视口中的运行检查局部滚动。

## 3. Verification

- [x] 3.1 运行完整前端测试套件和 `npm run build`。
- [x] 3.2 运行 `go test ./...`、`go build ./...`、`git diff --check` 和 `openspec validate dashboard-first-screen-fit --strict`。
- [x] 3.3 向用户呈报完整验证结果，获得确认后再归档变更。
