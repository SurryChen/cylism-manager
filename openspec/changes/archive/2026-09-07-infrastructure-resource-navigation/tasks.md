## Implementation

- [x] 新增基础设施聚合页面与页内视图切换。
- [x] 更新基础设施侧栏和移动端导航。
- [x] 将旧细分路由重定向到新聚合路由。
- [x] 为导航、初始视图和旧路由重定向补充前端测试。
- [x] 执行前端测试与构建。
- [x] 将聚合导航调整为紧凑上下文栏，并移除侧栏重复模块标记。
- [x] 为紧凑上下文栏补充前端测试与构建验证。
- [x] 将集群视图改为单层平铺标题栏，并为无分栏页面复用标题栏结构。
- [x] 为平铺集群视图与无分栏标题栏补充前端测试与构建验证。
- [x] 移除全局当前工作区伪元素，并消除集群节点页的重复子标题。
- [x] 为服务器、存储卷和监控应用共享无分栏标题栏，并修复集群内容区间距。
- [x] 固定全部基础设施标题栏高度，并移除存储卷与监控标题栏中的说明文字。
- [x] 将存储卷调整为集群级 PVC 总览，并将项目和环境改为可选筛选。
- [x] 支持在指定命名空间创建独立托管 PVC，并保持应用挂载与危险操作的安全边界。
- [x] 将服务器页面拆分为基本配置与批量实时资源监控视图。
- [x] 为资源采集接口补充受限并发、失败状态和前后端回归测试。
- [x] 将集群监控拆分为概览、节点、工作负载和诊断视图。
- [x] 新增受限的 VictoriaMetrics 历史查询接口，并为范围校验补充后端测试。
- [x] 为节点 CPU、内存、根磁盘和网络趋势新增可复用图表组件与前端测试。
- [x] 为 VictoriaMetrics 采集目标补充节点标签，并提供已安装实例的配置同步入口。
- [x] 将概览趋势改为服务端批量查询，移除页面定时轮询并按子页按需加载诊断数据。
- [x] 支持以节点 InternalIP 兼容无 `node` 标签的历史序列，并将时间范围调整为选择控件。
- [x] 执行监控页、后端和前端全量测试及构建验证。
- [x] 将节点趋势收敛到概览视图，并提供多节点筛选，保持分栏数据按需加载。
- [x] 将诊断能力收敛到监控设置抽屉，使用多选下拉筛选趋势节点，并支持在线更新指标保留天数。
- [x] 执行安全扫描（本机未安装 `~/.security-scan/bin/sec-code`）。
