## 1. 产品与信息架构基线

- [ ] 1.1 更新产品文档与概览文案，明确“单 control-plane K3s 控制台”定位
  - 验证：产品文档与概览页面不再将 NGINX/acme.sh 作为主叙事
- [ ] 1.2 重构导航结构，基础设施仅保留 `/servers`、`/cluster`、`/workloads`、`/services`、`/configs`、`/routes`、`/certs`
  - 验证：前端路由与导航测试覆盖精简后的入口及选中态
- [ ] 1.3 将 `db-admin` 从主路径降级为内部维护入口
  - 验证：普通导航中不出现“数据管理”主入口，但内部直达路由仍可访问

## 2. 服务器纳管模型重构

- [ ] 2.1 调整 `Server` 模型，移除 Agent/心跳导向字段，补齐激活与 Tailscale 纳管字段
  - 验证：模型迁移后服务端测试通过，旧字段不再出现在 API 响应
- [ ] 2.2 新增 `ServerFactCache` 与系统设置持久化能力
  - 验证：可写入并读取最近一次主机事实缓存与系统设置
- [ ] 2.3 以 TDD 方式重写服务器列表、详情、编辑和删除逻辑
  - 验证：`go test ./internal/...` 中服务器相关测试覆盖 CRUD、脱敏与状态字段

## 3. Tailnet 发现与导入

- [ ] 3.1 基于宿主机 `tailscaled` socket 实现本机 tailnet 状态读取
  - 验证：API 返回本机在线状态、控制面 Tailscale IPv4 和 peer 数量
- [ ] 3.2 实现 tailnet peer 导入预览、冲突检测与严格模式
  - 验证：测试覆盖“无冲突导入”“部分冲突预览”“严格模式整批失败”
- [ ] 3.3 将导入入口与组网配置收敛到服务器页或系统设置，而不是独立页面
  - 验证：基础设施导航中不存在独立“组网引导”页面，仍可完成 Tailscale 相关配置入口访问

## 4. 服务器激活与主机事实

- [ ] 4.1 实现 SSH 激活检测，支持 root 与 sudo 两种权限模式
  - 验证：测试覆盖凭据错误、sudo 不可用、检测通过三类场景
- [ ] 4.2 实现按需拉取的实时资源查看与 15 秒短缓存
  - 验证：打开服务器详情可看到 CPU/内存/磁盘信息，重复请求命中缓存

## 5. 集群节点闭环

- [ ] 5.1 将节点列表页面与服务器页面完全拆分，节点信息只来自 K8s API
  - 验证：前端页面不再在服务器页签中展示节点表格
- [ ] 5.2 以 TDD 方式重写 worker 加入流程，只允许从“已激活且未入集群”的服务器中选择
  - 验证：测试覆盖 join 成功、join 失败、节点 Ready 超时
- [ ] 5.3 完成节点映射、Cordon/Drain/Remove 与异常态展示
  - 验证：测试覆盖映射成功、映射失败进入 `drifted`、drain/remove API 调用成功

## 6. Kubernetes 资源页面补全

- [ ] 6.1 调整工作负载页面，支持按命名空间、节点、服务关联查看
  - 验证：前端测试覆盖三种视图切换
- [ ] 6.2 保持服务、配置、路由页面以 K8s 资源为中心，不回退到宿主机对象模型
  - 验证：接口与页面测试不再依赖站点/NGINX 术语

## 7. 证书与扩展

- [ ] 7.1 将证书页面改为扩展感知入口，未安装 cert-manager 时展示引导状态
  - 验证：前端测试覆盖“已启用证书管理”和“未启用扩展”两种页面状态

## 8. 审计与验收

- [ ] 8.1 将导入服务器、更新凭据、激活服务器、加入 worker、查看 Secret 明文等操作纳入审计
  - 验证：操作后审计日志包含 user、action、resource、result
- [ ] 8.2 全量验证
  - 验证：`go test ./...`、`go build ./...`、`cd web && npm test`、`cd web && npm run build` 全部通过
