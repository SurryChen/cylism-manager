package bootstrap

// Service construction currently remains delegated to api.RegisterRoutes so
// the migration can be performed incrementally without changing route
// behavior. This file is the stable home for future domain service wiring.
