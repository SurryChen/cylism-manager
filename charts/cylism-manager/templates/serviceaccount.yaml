{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cylism-manager.serviceAccountName" . }}
  labels:
    {{- include "cylism-manager.labels" . | nindent 4 }}
{{- end }}
